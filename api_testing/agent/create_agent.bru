meta {
  name: create_agent
  type: http
  seq: 1
}

post {
  url: {{API_ORIGIN}}/agent/create
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  name: new chat 2
}

settings {
  encodeUrl: true
  timeout: 0
}
