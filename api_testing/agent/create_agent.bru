meta {
  name: create_agent
  type: http
  seq: 1
}

post {
  url: {{API_ORIGIN}}/agent
  body: json
  auth: inherit
}

body:json {
  {"name": "new chat 16"}
}

body:text {
  {
    "name": "new chat 16"
  }
}

body:multipart-form {
  name: new chat 4
}

settings {
  encodeUrl: true
  timeout: 0
}
